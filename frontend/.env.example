# Base URL of the ImageAudit FastAPI backend (POST /analyze, GET /history, GET /results/:id).
# Defaults to http://localhost:8000 for local development.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
